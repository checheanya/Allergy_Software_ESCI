@startuml
start
:User opens app;
:App shows login screen;
:User selects "Log in with Facebook";
:App performs Facebook login;
if (Login is successful) then (yes)
  :App creates a new user account;
  :App logs in the user;
  :App shows home screen;
else (no)
  :App displays error message;
  end
@enduml
